# Tasks

- [x] 1. IArtifactMergeService + ArtifactMergeService (synthetic source + accepted MergeArtifact proposal via IProposalApplicator)
- [x] 2. POST /api/worlds/{worldId}/artifacts/{targetId}/merge + DI
- [x] 3. Merge section on artifact detail (GM-only autocomplete + confirm + navigate)
- [x] 4. Service tests with the real applicator (role gate, self-merge, cross-world, facts-move/archive/provenance)
