## List of unprocessed features

* Demo world
* New user onboarding
* Can we drag storylines in the tree view and then have it update in place instead of reloading the whole page?
* Can I pin the list of storylines in the tree view so that only the git tree part scrolls?
* Make artifact details stretch the full width of the page
* On the source details page, let's add a section that lists all extracted knowledge and a prompt box that allows a GM (only) to remove facts that are incorrect.
* On the capture page, the Captured On box is marked optional, but I cannot delete the default date to put in something undated
* Locations
	* The Locations feature loads SLOWLY, let's figure out how to speed it up
	* The map pins don't properly overlay the image - perhaps it's because the image is being scaled to the viewport?
	* I want to rethink the locations feature - it doesn't feel as useful as it might otherwise be. But perhaps that's a data quality issue, related to how notes were imported and their associated dates
	* The extraction AI didn't properly understand where we were in notes unless specifically mentioned in the note. Is there a way for it to understand location context at the time of note import?