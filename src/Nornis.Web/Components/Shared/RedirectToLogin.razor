@inject NavigationManager Nav

@code {
    protected override void OnInitialized() =>
        Nav.NavigateTo($"/account/login?returnUrl={Uri.EscapeDataString(new Uri(Nav.Uri).PathAndQuery)}", forceLoad: true);
}
