@page "/settings"
@inject NavigationManager Nav

@* Settings moved into the GM-only Admin page (Settings tab). This stub keeps old
   bookmarks working. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/admin", replace: true);
}
