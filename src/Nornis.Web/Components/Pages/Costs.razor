@page "/costs"
@inject NavigationManager Nav

@* Costs moved into the GM-only Admin page (Costs tab). This stub keeps old
   bookmarks working. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/admin", replace: true);
}
